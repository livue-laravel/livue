<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="csrf-token" content="{{ csrf_token() }}">

    <title>{{ $title ?? config('app.name', 'Laravel') }}</title>

    {{-- Prevent FOUC (Flash of Unstyled Content) with Vue UI frameworks --}}
    <style>[v-cloak] { display: none; }</style>

    {{-- LiVue assets are auto-injected here. DO NOT move your assets above this marker. --}}
    <!-- livue-assets -->

    {{-- Your application assets go AFTER the livue-assets marker --}}
    @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body>
    {!! $slot !!}
</body>
</html>
